syntax = "proto3";

message SensorData {
    message DataPoint {
        int32 index = 1;
        float val = 2;
    }

    message DataRecord {
        int64 time = 1;
        repeated DataPoint data = 2;
    }

    repeated string sensors = 1;
    repeated DataRecord datas = 2;
}