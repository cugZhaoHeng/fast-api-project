syntax = "proto3";

package example;

// 用户消息定义
message User {
    int32 id = 1;
    string name = 2;
    string email = 3;
}

// 请求消息
message CreateUserRequest {
    User user = 1;
}

// 响应消息
message CreateUserResponse {
    User user = 1;
    bool success = 2;
}